/**
 * A stand-in Agent.bx whose `detectEnvironment()` returns something that is not
 * a simple value - treated as "no opinion", not as an error.
 */
class {

	/**
	 * @return A struct, deliberately not an environment name
	 */
	any function detectEnvironment() {
		return { not : "a name" };
	}

}
