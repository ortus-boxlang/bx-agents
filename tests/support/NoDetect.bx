/**
 * A stand-in Agent.bx that declares NO `detectEnvironment()`, so
 * EnvironmentResolver must fall through to its next tier.
 */
class {

	/**
	 * @return An empty config struct - this descriptor exists only to NOT have detectEnvironment()
	 */
	struct function configure() {
		return {};
	}

}
