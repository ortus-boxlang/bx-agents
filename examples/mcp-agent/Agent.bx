class {

	function configure() {
		return {
			name        : "mcp-agent",
			model       : "mock/mock-model",
			description : "An agent that hosts a local MCP server re-exposing one of its own tools."
		};
	}

}
