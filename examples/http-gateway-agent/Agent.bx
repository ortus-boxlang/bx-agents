class {

	function configure() {
		return {
			name        : "http-gateway-agent",
			model       : "mock/mock-model",
			description : "An agent exposed over HTTP via gateways/ + toAi()."
		};
	}

}
