class {

	function configure() {
		return {
			name        : "telegram-gateway-agent",
			model       : "mock/mock-model",
			description : "An agent reachable as a real Telegram bot via gateways/."
		};
	}

}
