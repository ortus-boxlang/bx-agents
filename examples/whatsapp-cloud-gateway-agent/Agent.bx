class extends="bxModules.bxai.models.runnables.AiAgent" {

	function init() {
		super.init(
			name        : "whatsapp-cloud-gateway-agent",
			description : "An agent reachable over WhatsApp via gateways/, using Meta's Business Cloud API webhook.",
			model       : aiModel( provider: "mock", params: { model: "mock-model" } )
		)
		return this
	}

}
