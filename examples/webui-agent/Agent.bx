class {

	function configure() {
		return {
			name        : "webui-agent",
			model       : "mock/mock-model",
			description : "An agent reachable through the v1 web chat UI via gateways/."
		};
	}

}
