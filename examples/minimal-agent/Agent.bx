class {

	function configure() {
		return {
			name        : "minimal-agent",
			model       : "mock/mock-model",
			description : "The smallest complete BX Agents project: one tool, one skill."
		};
	}

}
