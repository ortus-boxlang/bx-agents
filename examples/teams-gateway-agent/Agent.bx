class {

	function configure() {
		return {
			name        : "teams-gateway-agent",
			model       : "mock/mock-model",
			description : "An agent reachable over Microsoft Teams via gateways/, using the Bot Framework."
		};
	}

}
