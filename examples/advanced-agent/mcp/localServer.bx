class {

	function configure() {
		return {
			description : "Advanced Agent's internal tools MCP server",
			version     : "1.0.0",
			cors        : "*",
			tools       : [ "getStockQuote", "convertCurrency" ]
		};
	}

}
