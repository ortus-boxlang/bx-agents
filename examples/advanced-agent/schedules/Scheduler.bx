class extends="coldbox.system.web.tasks.ColdBoxScheduler" {

	function configure() {
		task( "nightly-digest" )
			.call( () => getInstance( "advanced-agent" ).run( "Summarize today's research findings." ) )
			.everyDayAt( "00:00" )
			.withNoOverlaps()
	}

}
