class extends="coldbox.system.web.tasks.ColdBoxScheduler" {

	function configure() {
		task( "nightly" )
			.call( () => getInstance( "full-project-agent" ).run( "cleanup" ) )
			.everyDayAt( "00:00" )
	}

}
