class extends="coldbox.system.web.tasks.ColdBoxScheduler" {

	function configure() {
		task( "heartbeat" )
			.call( () => getInstance( "e2e-coldbox-route-agent" ).run( "heartbeat" ) )
			.everyMinute()
	}

}
