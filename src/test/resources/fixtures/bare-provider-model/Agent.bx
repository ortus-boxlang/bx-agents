class {

	function configure() {
		return {
			name        : "bare-provider-agent",
			description : "An agent whose model is a bare provider name (no models/ entry, no slash)",
			model       : "openai"
		};
	}

}
