class {
	function configure() {
		return {
			type               : "whatsapp-cloud",
			accessTokenEnvVar  : "MY_WA_ACCESS_TOKEN",
			phoneNumberIdEnvVar: "MY_WA_PHONE_NUMBER_ID"
			// appSecretEnvVar and verifyTokenEnvVar deliberately omitted
		};
	}
}
