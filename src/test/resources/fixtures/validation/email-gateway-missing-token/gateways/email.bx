class {
	function configure() {
		return {
			type          : "email",
			imapHostEnvVar: "MY_IMAP_HOST",
			imapUsernameEnvVar: "MY_IMAP_USERNAME"
			// imapPasswordEnvVar and fromAddressEnvVar deliberately omitted
		};
	}
}
