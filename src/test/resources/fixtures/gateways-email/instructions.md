# Gateways Email Fixture

Used to test GatewayGenerator's push-style gateway wiring for Email
(registration via a bare class path, plus the generated
interceptors/GatewaySessionBootstrap.bx and moduleSettings.cbmailservices).
