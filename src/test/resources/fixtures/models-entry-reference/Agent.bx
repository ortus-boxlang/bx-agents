class {

	function configure() {
		return {
			name        : "models-entry-agent",
			description : "An agent whose model references a models/ entry by name",
			model       : "summarizer"
		};
	}

}
