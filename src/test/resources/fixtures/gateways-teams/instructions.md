# Gateways Teams Fixture

Used to test GatewayGenerator's push-style gateway wiring for Microsoft
Teams (registration via a bare class path, the generated handlers/Teams.bx
webhook handler, and interceptors/GatewaySessionBootstrap.bx).
