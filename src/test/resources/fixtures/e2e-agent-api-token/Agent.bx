class {

	function configure() {
		return {
			name        : "token-gated-agent",
			description : "Exercises agentApi.tokenEnvVar's generated withCondition gate over real HTTP",
			model       : "mock/mock-model",
			agentApi    : {
				tokenEnvVar : "BXAGENTS_E2E_TOKEN"
			}
		};
	}

}
