# Gateways WhatsApp Cloud Fixture

Used to test GatewayGenerator's push-style gateway wiring for WhatsApp
Business Cloud API (registration via a bare class path, the generated
handlers/WhatsAppCloud.bx webhook handler, and
interceptors/GatewaySessionBootstrap.bx).
