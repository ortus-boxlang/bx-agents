class extends="bxModules.bxai.models.runnables.AiAgent" {
	function init() {
		super.init( name: "MixedRoot", instructions: "root from class", model: aiModel( provider: "mock" ) )
		return this
	}
	function configure() {
		return { subAgents: [ "researcher" ] };
	}
}
