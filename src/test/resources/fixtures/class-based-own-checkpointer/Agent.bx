class extends="bxModules.bxai.models.runnables.AiAgent" {
	function init() {
		super.init(
			name        : "OwnCheckpointer",
			instructions: "i bring my own checkpointer",
			model       : aiModel( provider: "mock" ),
			checkpointer: aiMemory( memory: "cache" )
		)
		return this
	}
}
