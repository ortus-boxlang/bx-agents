# Gateways GitHub Fixture

Used to test GatewayGenerator's push-style gateway wiring for GitHub
(registration via a bare class path, the generated handlers/GitHub.bx
webhook handler, and interceptors/GatewaySessionBootstrap.bx).
