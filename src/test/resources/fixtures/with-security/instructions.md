# With Security Fixture

Used to test that Agent.bx's `security: {}` block survives config
resolution (and environment overrides) unchanged, for verbatim passthrough
into the generated app's module settings.
