# Gateways Twilio Fixture

Used to test GatewayGenerator's push-style gateway wiring for Twilio SMS
(registration via a bare class path, the generated handlers/Twilio.bx
webhook handler, and interceptors/GatewaySessionBootstrap.bx).
